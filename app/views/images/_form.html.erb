<%= form_for image, url: images_path, html: { multipart: true } do |f| %>
  <%= f.file_field :pic %>
  <%= f.hidden_field :project_id, value: project.id %>
  <%= f.submit 'post image', class: 'button expanded' %>
<% end %>
