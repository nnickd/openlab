<table class="hover">
    <thead>
        <tr>
            <th>Project</th>
            <th>Amount</th>
        </tr>
    </thead>
    <tbody>
        <% payments.each do |payment| %>
        <tr>
            <td><%= payment.pool.project.title %></td>
            <td><%= payment.amount %></td>
        </tr>
        <% end %>
    </tbody>
</table>
