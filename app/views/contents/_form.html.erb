


<%= form_for(content) do |f| %>
<%= f.text_area :body, placeholder: "body", class: "text-center"  %>
<%= f.hidden_field :project_id, value: content.project.id %>
<%= f.hidden_field :kind, value: kind %>

<%= f.submit 'post', class: "button expanded large" %>
<% end %>

