<div class="column">
    <% projects.each_slice(3) do |project| %>

    <div class="row align-spaced">

        <% project.each do |pro| %>

        <section class="column small-3" id=<%= pro.id.to_s %>>

            <div class="row">
                <%= image_tag 'galaxy.jpg' %>
            </div>

            <div class="row callout">
                <h3>
                    <%= pro.title %>
                </h3>
            </div>

            <div class="row callout">
                <%= render 'pools/thumb', pool: pro.pool %>
            </div>

        </section>
        <% end %>

    </div>
    <% end %>

</div>
