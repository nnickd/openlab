<%= form_for(project) do |f| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :about %>
    <%= f.text_area :about %>
  </div>

  <div class="field">
    <%= f.label :context %>
    <%= f.text_area :context %>
  </div>

  <div class="field">
    <%= f.label :significance %>
    <%= f.text_area :significance %>
  </div>

  <div class="field">
    <%= f.label :goals %>
    <%= f.text_area :goals %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
